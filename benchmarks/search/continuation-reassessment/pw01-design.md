# PW01: cancelled qualification proposal

**Paused by the user before publication or execution. Zero PW01 emulator frames.
This draft is not an active allocation and must not be executed.**

The proposal is archived here at the user's request to preserve durable work.
It was never published as an active allocation. The runner rejects this cancelled
registration before host access or process creation; an offline test exercises
that refusal. The planned protocol below is historical, not an instruction to
resume. Build records and the verified ms02 cleanup receipt are indexed by
pw01-preparation-manifest.json. The remote working directory has been removed.

Source adc8e70394a97e212fd36fdd002b48761808df7d is published and source-qualified
in progress-word-design.md and pw00-source-manifest.json. A separate bounded ms02
build produces the binary frozen in pw01-registration.json. Its build performs
zero emulation; all prior runtime allocations remain closed. No PW01 emulator
execution has occurred at this publication.

The cancelled proposal specified three sequential cells from the qualified supplied root,
using one new frozen seed2871049831 and250,000 admitted frames per cell:
ordinary alphabet, queued fresh-action control, and queued productive-word reuse.
All use identical progress retention, legacy selector, feature build, actions,
512MiB logical memory, four workers on CPUs4–7, and physical accounting. The
request omits the mixture field in the ordinary arm, qualifying that default's
real serialized contract. No previous failed panel seed is reused.

Each cell stops at a living Ridley defeat or the fixed frame horizon. Its20,000
execution/60second search caps are safety stops, not substitutes for attaining
the horizon. Child processes have90seconds; the service has300seconds,4GiB,
zero swap and64tasks. Total output is capped at1GiB,128MiB per file. Proposed
allowance was750k pre-drain search and7.6M complete physical frames, with the
explicit constructor/direct-helper/drain derivation in the registration. The
absolute native deadline is20minutes after registration, synthesis35minutes.
Unused work is released. There are no retries or seed replacements.

All three cells must satisfy real serialized policy, root/asset/source identity,
complete campaign/checkpoint replay, two local and two full-prefix witness
replays, living witness and exact physical-cost gates. All must actually admit
progress alternatives. The two queued modes must share all prior stream records
until a first differing continuation action, keeping parent, seed, worker,
sequence, selector and donor/leaf learning identity equal; the candidate's leaf
must be its parent. This gate tests activation of the compiled mechanism.
No difference, incomplete cells or invalid receipts close qualification without
another seed. Outcomes and CPU/RSS are recorded, but no efficacy comparison is
made or promoted from this one supplied-state seed.

Three offline tests exercise the actual earlier progress report schema, clearly
synthetic new policy IDs, omitted ordinary mixture, and planted policy, replay,
work, horizon, witness and first-divergence failures. Source campaign fixtures
already exercise the real new paths, pressure, workers/buffers and corrupt tails.
The runtime wrapper preserves cell usage/process records and terminal cgroup
receipts before collection, and stops after an invalid cell. Every launched
cost remains charged. Frozen earlier scorers and negative outcomes are unchanged.

A pass only permits designing a separately registered conditional endpoint
screen with matched action and ordinary controls. No fresh development,
independent confirmation, transfer or untouched panel is allocated. There is
no control promotion, historical-data pooling or horizon extension. The full
fresh-search goal remains active and unachieved. msr1 must not be accessed.
