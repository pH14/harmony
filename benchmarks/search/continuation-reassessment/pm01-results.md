# PM01 closes the completed-run physical-work gap

The single registered ms02 measurement **passes** in **18.271 seconds**. It records
exactly the prospective **983,632 physical frames**. All eight reference artifacts
are byte-identical to the original PG01 progress cell: campaign stream, report,
origin, final checkpoint, root report/snapshot and both selected witness inputs.
The full campaign/checkpoint and witness replay checks pass again, with the same
70 alternative admissions and no living boss defeat. This qualifies accounting;
it is not another independent efficacy sample.

## Measured components

| Component | Physical frames | What it includes |
| --- | ---: | --- |
| Direct helpers | 477,418 | Both root replays and four selected-witness replays; 5,574 setup frames included once |
| Search engine | 254,965 | 250,320 admitted frames plus five constructors totaling 4,645 frames |
| Full campaign replay | 251,249 | 250,320 replayed admission frames plus one 929-frame constructor |
| Total | **983,632** | Sum of the three disjoint phases |

Every engine construction attempt succeeds and every measured target lifetime
closes. No clock anomaly occurs. Closed post-constructor deltas equal admitted
work exactly in both engine phases. Thus, for this completed run, reconstruction
is already included and the omitted engine work was precisely **5,574 constructor
frames**. The independent lifetime sum confirms the source-derived identity; it
uses the same backend clock as job deltas, observed at different boundaries.
It is not a second hardware timing source.

The meter adds no campaign fields or policy feedback. It makes no extra clock
reads when omitted and no per-frame locking when enabled. Host overhead can still
affect a wall-time cutoff. Failed constructors, live target lifetimes, clock
regression/saturation, arithmetic overflow or externally interrupted receipts
cannot be treated as complete totals. QuickNES's pinned destructor does not run
frames; host initialization/deinitialization work remains visible in CPU/wall
measurements, outside the explicit emulated-frame metric.

## Identity, costs and closure

The tested source is `e562e17840cc10fbd3901005ce23df2ed928acda`, built with
`metroid-retention-progress`. Its binary SHA256 is
`400ad54eb044917cf81b0981721c399b203334a2c1de3735b134cac13b64c807`.
The compile-only build completes in 18.149 seconds. Registration `33132749` is
published before execution, with normal hooks. No native run is retried.

The process runs from 2026-09-10T21:42:46.879052Z through 21:43:05.149660Z. It uses
21.467 CPU-seconds and peaks at 58,568 KiB RSS. Service CPU is 21.507 seconds and peak
memory 83,210,240 bytes, with no swap. Effective limits are CPUs 0–3, 4 GiB, 64 tasks and
180 seconds; the process has 120 seconds. Its original search/direct limits and all
prior PG01 parameters stay fixed. The registered 3M-frame allocation is respected.

The completed service is explicitly stopped and is inactive/dead with MainPID 0.
Unused allocation is released. PM01 charges 250,320 admitted frames and 733,312 known
auxiliary frames, which sum to the complete 983,632 physical frames. Search setup
belongs in the auxiliary charge; replay setup belongs in the full replay charge.
The resumed ledger is now **2,983,485,644 admitted / 71,300,232 known auxiliary frames**.
Historical incomplete components remain as originally recorded; this experiment
does not manufacture retrospective measurements. msr1 was not accessed.

## Reproduce and advance

All 17 exact native files, independent ms02 hashes, source/request bindings, eight
PG01 artifact comparisons, lifetime arithmetic, resource limits, service closure
and ledger verify without an emulator:

```sh
python3 benchmarks/search/continuation-reassessment/verify_pm01.py
```

The source suite passes 167 generic tests plus its interface test, 154 NES library
tests, both five-test caller variants and strict Clippy. Three planted native-gate
checks reject omitted/double-counted setup and incomplete/unexplained work. Source
tests also cover write failure before recorded jobs, restore/reset, live receipts,
regressed/saturated clocks and overflow. No unsafe code changed.

This removes the accounting prerequisite for a separately registered conditional
living-boss-defeat screen. `pg02-design.md` defines the next comparison and its
futility rule; it is not an allocation or a seed registration. The qualified
retention rule, projection and action law stay fixed. The full fresh-search,
independent-confirmation, MM2-transfer and untouched-validation goal is active
and unachieved. All earlier failed gates remain closed.
