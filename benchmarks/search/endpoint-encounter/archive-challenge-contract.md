# Existing archive engine, original encounter

S01/S02 established two surviving damage endpoints, with complete-prefix replay,
but neither is a fresh victory or evidence of a generic policy gain. Do not
extend the manually selected chain again. The next capability question uses the
original E01 encounter (boss HP140, health79 tenths, no missiles), unchanged
resources, the ordinary selector/action law and the existing archive engine.
A future comparative panel must separately specify equal physical-work/memory
ceilings and its fixed-root comparator; no superiority claim follows from an
archive-only capability pilot. No pilot is allocated by this contract.

Use the existing `CampaignOrigin::SnapshotRoot`; do not implement another search
algorithm. The small standalone challenge command leaves the default evaluator,
archive, target and frozen diagnostic binaries unchanged. All archive actions
and retained inputs are root-relative. Bind the supplied prefix, assets, raw
context, emulator state and complete snapshot to exact hashes. A prepare-only
mode establishes the snapshot digest without launching search; run requires
that digest. The original searched prefix cannot be injected as a splice donor.

Qualify first, in a separately registered maximum 4M known-auxiliary block:
one prepare-only two-replay check and two fixed 16-job campaigns differing only
in one/two result-buffer slots, with the same seed and four workers. Both require
full campaign replay, exact same-host stream/checkpoint and endpoint identity,
and two root-relative plus two complete-prefix witness replays. The root must
match E01's exact qualified context and emulator bytes. Preserve setup and actual
helper frame costs. Native engine setup/unadmitted/reconstruction costs remain
unknown rather than being labeled zero; qualification is not a throughput claim.
Each cell has a hard external process/output/time limit and bounded direct work.
No parameter or root replacement follows a failed check.

The wrapper observes a registered Kraid/Ridley defeat only after execution zero,
within the admitted frame limit, and requires a live full-prefix witness with
the corresponding actual defeat flag. A wall-censored run remains incomplete.
An existing root event, intermediate/failed/dead state or later drained event
cannot become a successful within-budget challenge. The witness may be the
champion if no qualifying defeat exists; label that result explicitly.

First pass planted checks for origin-zero and budget-edge events, exact prefix
concatenation and direct-frame overflow. Then freeze source, native build and
prepare request before any emulation. Commit the resulting snapshot digest and
final tiny-campaign requests before their dispatch. No native performance run,
new reward, retention field or controller change is earned merely by adding
this adapter. After qualification, use its actual cost to register the smallest
capability pilot that can decide the next generic-policy hypothesis.
