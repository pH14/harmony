# AP01: unchanged archive capability at the original encounter

AQ01 qualifies the original E01 root, existing engine, full campaign replay and
complete-prefix witness construction. Register one capability pilot before
choosing any policy change: ordinary Metroid selection, independent one-to-six
actions, corrected terminal semantics and the original HP140 / health79-tenths
state. Do not start at either later manually selected damage endpoint.

Reuse qualification seed 2026090902, without replacing it after outcomes. The
16 qualification jobs are nested development observations, not independent
confirmation. Allocate a fixed 250,000-admitted-frame horizon, at most 5,000
jobs, four workers, 512 MiB logical archive, 120-second search, 300-second outer
process cap, 2 GiB process memory and 512 MiB output. The unchanged native binary
requires complete campaign replay and four witness replays. Direct helper work
is capped at 2M frames; the separate total known-auxiliary ceiling is 3M, including
admitted search and replay-admitted work. Keep engine setup, unadmitted and
reconstruction costs explicitly unknown. This is not a matched-work comparison.

The sole useful success is a surviving `ridley_defeated` event after execution
zero and within the frame horizon, with two root-local and two complete-prefix
replays. A completed negative requires the frame horizon; execution or wall
censoring cannot be relabeled a fixed-horizon negative. Preserve any failure
and actual work. No retry, larger same-policy sweep, new seed list or third
manually selected root follows automatically.

A positive establishes conditional capability for unchanged archive search in
an initially small supplied archive. It does not prove a global allocation
cause, improved fresh discovery or the benefit of a new generic policy. A
negative leaves cause unresolved; inspect the retained checkpoint and recorded
evidence before choosing a change. Only a separately justified, registered
comparison can establish a generic improvement. Fresh untouched validation and
MM2 transfer remain unearned until the original goal's gates are satisfied.
