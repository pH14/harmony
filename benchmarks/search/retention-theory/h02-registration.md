# H02: align the diagnostic horizon with the search job

Registered after U01, before collecting prefix outcomes. This is a bounded
diagnostic of a newly identified interpretation gap, not another retention
campaign. The U01 implementation phase already qualified before its cutoff.

The campaign uses a uniform one-to-six-action suffix; U01 measured24-action
suffixes. A long-horizon difference need not be visible before the next archive
decision. Separately, none of the four rejected U01 candidates with missing
map events is a strict input continuation of either local survivor. The data
does not support describing those losses as merely waiting longer on that tape.

Keep U01's exact seven competitions, all16 suffixes, seed20261215 and terminalv3.
Extend the standalone probe with an opt-in prefix sidecar. Execute the same
24-action suffixes once while recording cumulative outcomes after actions1–6.
After an early terminal, later prefix outcomes repeat that terminal result and
its unchanged cumulative frame count. The historical summary, suffix bank and
full-horizon outcomes must be byte-identical to the frozen U01 outputs; any
mismatch blocks interpretation. Default invocations retain the old output.

One frozen ARM diagnostic run,CPU8, at most3,474,703 physical frames including
all possible exports,120s watchdog and64MiB output. Prefix counters are
cumulative observations of that run, never additional physical-work totals.
Finish by08:35UTC or preserve the failure and stop;08:46UTC consolidation and
the10:16UTC tranche boundary stay fixed. No held-out validation or fresh search.

For every prefix horizon1–6, compute the complete two-survivor union, keeping
map/capability events and endpoint survival separate. Verify the repeated
unretained outcomes still agree. Report each horizon, plus the descriptive
average under the actual uniform length mixture; nested horizons are dependent,
so this is not a confidence interval. Recompute retrospective minimum covers
both at each horizon and jointly with horizon as an explicit event condition.
No new online policy or longer-job campaign follows automatically. This decides
whether the measured loss concerns immediate local exploration, later futures,
or both; it does not prove improved adaptive search.
