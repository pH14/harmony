# P05: descriptor check on the fresh seed's recorded competitors

R05 failed both fresh-search comparisons. This diagnostic asks whether the
frozen motion partition separates useful continuation differences in that new
seed's ordinary-retention competitors. It cannot reopen the failed motion
family, qualify a replacement policy or count as held-out search validation.

Use all sixteen equal-resource/preference pairs in stratum3 of R05 ordinary's
existing audit, in its recorded order. The full source audit SHA256 is
`d7c2e2c30741d1bd7a7a44384ee57cb1817a01c6d85e81a0fee69be218e236bd`.
No feature or outcome selects a pair. Only numeric input lengths and route
costs have been inspected before this registration. Preserve the full audit;
make a separate subset file with other strata empty. The source campaign uses
one representative per slot, so each recorded competitor is the complete
incumbent set for that original decision. This still does not answer the
two-survivor marginal-coverage question.

First use frozen`motion-probe-002` (SHA256
`d48042d84b6f055773131046c66a55bc522a3f92cba4c7304626e5a0a793a08c`)
to reconstruct both endpoints twice and check raw/cache/campaign-key agreement
and snapshot noninterference. Its existing2M physical-frame cap applies;
recorded route costs predict1,440,708 frames including the observed929-frame
setup. A mismatch stops the diagnostic before suffix work.

Then use frozen`probe-001` (SHA256
`20f0255bc4002d4e43ffecbfd9e0efc053c7656c2b06272bc4fbc16e7eff9672`),
corrected terminal v3, and new suffix seed20261214. Apply exactly16 shared
suffixes of24 actions to each side, preserving the existing sampled controller
vocabulary and gain-export behavior. This seed is fixed before generating or
viewing the suffix bank. No new search, trained policy, descriptor tuning or
import into fresh search is involved.

Before executing either helper, compute a conservative suffix-helper work bound
from every selected tape: setup allowance4096 frames per target, every input's
bounded held-frame sum, all16*24*120 maximum suffix frames per branch, and a
full setup/prefix/suffix export for every possible discarded-only gain. The
pinned target's668-frame boot prefix plus at most1200 waiting frames fits the
4096 setup allowance. Require this total to be at most12M frames. This is a
worst-case allowance including all potential gain exports, not an expected
cost. Record actual prefix, suffix and export frames afterward. The separate
motion helper can add at most2M. No setup or export is free in this bound.

Each helper gets120 wall seconds on core8, with a64MiB output cap. The enclosing
service gets300s and4GiB, kills its own process group on failure and preserves
partial output. No retries, more pairs, additional suffixes or longer budget
on an uninformative or incomplete result. Endpoint disagreement or an exceeded
work/output bound is a diagnostic failure, not evidence about search quality.

Report a pair-level table crossing equal/different frozen motion contexts with
presence/absence of a useful suffix disagreement. A useful disagreement is an
exclusive living map visit, different terminal survival, or an exclusive
equipment/capacity/boss gain, using the existing probe fields. Also report both
directions separately for survival, map/suffix events and capability gains;
do not choose a favorable weighting. Record exact mechanical equality and
full seven-byte motion equality as descriptive checks, without expanding the
frozen context tuple. No confidence or population-rate claim follows from
sixteen selected pairs sharing sixteen suffixes.

Same-context counterexamples can strengthen the case for action-conditioned
behavioral measurements. Different contexts with matching tested futures show
that a split alone is not observed marginal utility. Either result informs
the next measurement design; neither reverses R05's failed escalation gate.

## Completed outcome

Both helpers passed. Motion reconstruction used1,440,708 physical frames in
82.05s; suffix probing used1,062,409 frames in60.55s. Total2,503,117 frames and
142.60s, below the registered limits. No capability gain or gain export occurred.
The conservative preflight suffix bound was9,621,970 frames including all
potential exports. The finite shared suffix bank has SHA256 recorded in the
[analysis](p05-analysis.json); [full metadata](p05-results.json) records both
frozen executables and actual costs.

Useful disagreements appeared in six of eight same-context pairs and six of
eight different-context pairs. Two same-context pairs also share all seven raw
motion fields, but their full mechanical endpoints differ, so those cases do
not isolate an unobserved world variable. Pair10 has identical mechanical state,
the same coarse context, different raw motion fields and three distinguishing
suffixes. None of these comparisons identifies motion as the sole cause.

Discarded states survive122 trials versus120 for survivors and expose23
exclusive map/suffix events versus15. These are separate finite objectives on
selected competitors, not a new retention-policy win or a population estimate.
The frozen analyzer leaves every escalation and breakthrough flag false.
