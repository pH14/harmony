# Useful-continuation follow-up

This follow-up starts from verified commit
`ca85b3fa8e8e1b70cd98ccd85903d29dfdfe9409` after the completed 12-hour tranche.
The user requested advice from Fable 5.1 at xhigh, a revised goal, and continued
execution. [The extracted advice](fable-advice.md) and
[consultation provenance](consultation.json) preserve that consultation. The
advisor had public research documents and no callable tools; its rendered Write
requests did not execute. Advice is not independent verification.

## Revised decision

Prioritize whether a discarded state supplies useful continuation value beyond
what the archive already knows. Keep Metroid primary and test MM2 through the
existing fresh chains' own living Wily 1 states before another chain. No active
retention implementation family yet. A new untouched panel requires a qualified
mechanism with causal lineage, exposure, replay, work, memory, and development
evidence. The original boss/Wily 4 breakthrough criteria remain unchanged;
intermediate measurements do not replace them.

Initial diagnostic ceiling: 25 million new physical emulator frames and 4 CPU
hours, including reconstruction and verification; compilation separate. This is
an agent-selected resource ceiling, not a new user time limit. Register bounds
before each costly command. Preserve all failures and do not auto-increase caps.

Accepted Fable recommendations: inspect existing artifacts first; distinguish
survivor-relative exits from archive novelty; analyze pairs rather than treating
suffixes as independent seeds; locate encounter/frontier bottlenecks; require a
local transfer gate before more chains; withhold new retention and validation
until useful continuation evidence supports them.

Corrections to the advice:

- Control differences across different seeds describe dispersion, not a matched
  null distribution or a defensible mandatory effect threshold.
- The 87 count is historical-control underflow endpoints eligible for retention,
  not all underflow observations. Corrected exclusion counts are already exported.
  A larger count across diverged searches is not evidence of over-exclusion.
- Finite all-mask follow-ups cannot prove class-wide terminal correctness;
  successful classification cannot attribute search regression to chance.
  A low endpoint count likewise cannot rule out a nonlinear R+T interaction.
- Position/resource associations in 16 selected pairs are exploratory, not a
  trained or validated retention predictor. Fewer than 12 favorable pairs does
  not make all counterexamples anecdotes; ties and effect sizes matter.
- No boss encounter in retained checkpoints proves nothing about forgotten
  branches. Historical reporting can only be extended where raw data or replay
  inputs remain. Area entry, encounter, damage, and defeat stay separate.
- Parent-job credit is not all outgoing exposure (#283). Different chain states
  or matching watermarks alone do not establish entry-state causation.
- A diagnostic stage boundary does not authorize route guidance or change the
  original breakthrough outcome. No user reconfirmation is needed for already
  authorized bounded research.

## F01 preregistration: frozen artifact reanalysis

Question: how broadly is P03's exit imbalance distributed, does it persist when
both suffixes survive, and what can the retained evidence establish about useful
novelty? Separately reconcile terminal endpoint counts and both MM2 chains.
Existing aggregate counts cannot answer these questions. This step consumes no
new emulator frames. Bound: one CPU, 2 minutes, 256 MiB input ceiling per compact
file, outcomes streamed, immutable output. No controller inputs leave ms02.

Inputs: exact P02 audit, P03 outcomes and suffix bank, final compact validation
and development packets. Require source hashes, all 16 pairs and exactly 64
unique trial indices per pair, source/outcome execution and replacement identity,
and recomputed original totals/work. Source comparisons include map/capability
identity, exact position, posture, resources, and input lengths.

Report per-pair directional set differences, alive/dead 2x2 outcomes, same-actual-
frame comparisons, and both-alive comparisons. Report ties and descriptive sign
counts; any exact sign-test number is conditional on sampled correlated pairs,
not population significance. Do not divide events by frames and call it equal
work. Identify what archive discovery history exists before choosing a novelty
measurement. Terminal counters preserve both observed and eligible meanings.

Decision rules: if differences vanish outside unequal survival, prioritize a
fixed-total-frame resampling test; if differences persist, test whether they add
archive-new destinations, still under fixed total physical frames. If every
observed destination was already known, reject these samples as evidence for
archive-new map reach, without claiming all future equivalence. Missing history
is unknown, not novelty. No outcome here alone qualifies a retention policy.

For MM2, compare both own-chain origin descriptors and actual retained living
frontier descriptors. Repeated visited watermarks do not identify valid probe
starts. Preregister an ordinary-suffix probe once the start inventory is known.

### F01 result and history limitation

F01 reproduces all frozen hashes and aggregate work/counts. Overall pair signs
are 9 discarded / 5 survivor / 2 ties. Both-alive trials have exits12/16 across
260 trials (pair signs2/3/11); equal-actual-frame trials14/24 across401 trials
(pair signs3/4/9). Survival outcomes D/S: alive/alive260, alive/dead197,
dead/alive13, dead/dead554. The aggregate asymmetry does not persist in the
both-alive subset. Those conditioned subsets are descriptive, not interventions.

The source and chain campaign exports do not contain full retained archives or
first-discovery membership histories; streams were hashed but not retained.
Fable's proposed checkpoint re-export and full MM2 archive sampling cannot be
performed from those files. We will use explicit lower bounds on known coverage
and trajectory-boundary sampling, without labeling them complete archives.

## F02 preregistration: equal physical probe work and known-source coverage

Question: after allowing a fragile survivor more ordinary attempts for the same
physical frames, does the discarded source add living map reach? Are any extra
destinations absent even from the two already discovered source trajectories?
F01's conditioned subsets cannot answer this counterfactual.

Freeze all16 P02 resource-tradeoff pairs, corrected terminal semantics, ordinary
sample_chord vocabulary/durations, and 24-action suffix horizon. Each side gets
exactly100,000 actual emulator frames, restarting from its source after terminal
or suffix completion. Both consume the same deterministic ordered suffix bank
(Romu seed0x616c746675747572), up to4096 suffixes. Death consumes actual work;
more attempts on the fragile side are the equal-work counterfactual. Truncate
only the last action at the physical cap. A side that exhausts the bank or cannot
make progress is incomplete, not a matched result. Preserve all outcomes.

Replay each source once, verify its full decoded state, and record all living
map visits. The union of the pair's two source paths is a lower bound on maps
already observed by that source campaign at replacement time. A map absent from
this union is **unknown against the full archive**, not globally novel. Report
per-pair union reach, directional differences, differences beyond known source
coverage, attempts, survival, capabilities, actual frames, prefix and bootstrap
work, snapshot sizes, suffix-bank and output sizes. No trajectory generated by
this diagnostic enters a fresh search. Reconstruct any claimed novel-destination
witness twice from ordinary genesis on independent targets before calling it
verified; those frames count separately.

Implementation: optional fixed-work mode in the development-only Metroid probe;
legacy CLI/output remains qualified. Focused tests/clippy <=10m; native build
separate; original P03 compatibility <=5m. F02 main run <=5m on CPU16, then a
second identical run <=5m for deterministic output and restoration qualification.
Full source reconstruction must fit2M declared action frames per run; probe
ceiling3.2M frames per run, max4096 attempts/side. Initial two runs plus compatibility
fit the follow-up's25M-frame ceiling before any witnesses. Log incomplete runs.

Decision: no extra discarded reach at equal frames rejects the sampled
fixed-budget advantage. Extra reach entirely in known-source maps rejects these
samples as support for archive-new map coverage. Extra reach outside that lower
bound is a lead requiring actual discovery-history sampling, not a retention
pilot. Capability gains require ordinary-genesis verification and a separate
causal gate. No depth claim or universal equivalence claim follows from a negative.

## F03 preregistration: MM2 own-trajectory frontier extendability

No complete MM2 archive survives. Sample action boundaries on each chain's own
Wily 1 champion trajectory instead; label the result as trajectory-conditioned.
Reconstruct from its exact own-chain stage prefix. From living, nonterminal,
menu-closed boundaries without the adapter's scrolling value0x80, choose up to8
first distinct spatial cells at the largest screen descriptor and up to4 first
distinct cells at the median distinct screen descriptor. Cell descriptor is
(stage, screen, room, x/16, y/16), used only for reporting. If median equals the
maximum, report missing separate calibration. No additional starts or retries
are selected based on outcomes. Source candidates and selected indices are
exported before probing. These filters do not prove player controllability.

From each selected snapshot, execute the same64 ordinary sampled suffixes of24
actions, Romu seed0x616c746675747572, with the existing MM2 policy. Report actual
frames, death, living cells, cells absent from the full champion trajectory's
observed cell set, maximum living non-scroll endpoint screen, boss/weapon gains,
and counts of emitted scrolling/menu observations. MM2 observations are sparse;
those counts are not durations. This is an extendability diagnostic, not an
equal-frame policy comparison or archive-global novelty claim. Automatic award
waits and genesis/prefix work count in physical work.

Run each chain once, <=5m/chain onCPU17, <=4M total physical frames/chain with a
reserved allowance for the adapter's bounded automatic wait. Keep at most12
snapshots, bound inputs to2MiB each/20k combined actions/2M declared frames.
Require successful endpoint reconstruction and snapshot restore, report failures
and missing calibration. A new boss/stage witness requires two independent
ordinary-genesis replays before any attainment claim. No fresh chain rerun.

Decision: frontier extension with calibration extension rejects physical
inextendability of these sampled states, but does not identify retention or
selection as the cause. Low frontier extension with positive calibration
prioritizes local continuation; zero on both is an uninformative diagnostic to
redesign, not a budget-increase trigger. Stable screen advance is a lead, not
Wily4 attainment. Apply no stage-specific route or controller guidance.

### F02 witness verification registration

The first fixed-work run exported15 witness files (including shared new-to-source
maps). Before any verified reach claim, reconstruct every exported file twice
on independent ordinary-genesis targets, require the recorded full decoded
endpoint and matching final snapshot hashes, and require every reported living
map from that suffix in both replay traces. The exact witness count is checked
from the immutable manifest; split into batches of at most16 if necessary.
Sources that end dead are valid witnesses of earlier living map visits; they
must not continue after terminal. Add a bounded source-only replay mode to the
probe so verification consumes no extra random suffix. <=5m per batch onCPU16,
<=2M declared action frames per batch (bootstrap additional), accounting all
physical frames. This does not establish full-archive novelty or fresh attainment.

F01 terminal reconciliation: historical controls observed87 underflow action
endpoints, all87 eligible; corrected arms observed3271, zero eligible. These are
counters on diverged trajectories, not independent tests of3271 false positives.
No evidence here establishes over-exclusion, and this follow-up changes no
terminal semantics. The mechanical source explanation and prior all-mask sample
remain bounded evidence; the resource-plus-terminal interaction remains untested.

F02 completed twice with exactly equal physical probe work on every side:
3.2M probe frames plus898,376 source/bootstrap frames per run. Full outcome and
summary equality passed. Discarded-only map reach appears in4 pairs, survivor-only
reach in2, and the map sets tie in10. Every discarded-only map was already on its
pair's source paths. The survivor alone has an extra destination outside known
source coverage in one pair; shared unknown-to-source destinations also occur.
This rejects the proposed archive-new-map benefit for these discarded sources
under the registered local sampler and work. It does not prove long-horizon
behavioral equivalence, reject all retention mechanisms, or establish global
novelty for the survivor. No retention implementation or fresh panel is qualified.

## F04 preregistration: verification qualification after review

The scientific outcome remains supported. Tighten the evidence binding: producer
snapshot hashes and prefix action offsets on every Metroid witness; verify maps
only after that offset, compare both independent replay snapshots to the producer,
and require the exact witness count. Record the fixed v3 verification policy.
Allow the source horizon plus at most128 suffix actions in source-only verification.
Require fixed-work competitors to share a starting map (all frozen16 already do).

For MM2, assert a single stage request, pin the already observed input hash, bind
its replay endpoint to the original chain witness snapshot, assert reset-origin
identity, report missing calibration explicitly, and bind any future gain input
to its own-chain prefix. Reset restores the emulator snapshot without clocking
frames; no omitted prefix re-emulation was found. Preserve the upper-median rule
and test its two-screen missing-calibration case; no existing sample set changes.

Qualification: focused tests and all-target Clippy <=10m, native builds separate.
Then original P03 compatibility, one exact F02 equal-work repeat, all15 witnesses
replayed twice with producer/suffix binding, and both F03 chains again. Each native
cell <=5m onCPU16 (Metroid) or17 (MM2), immutable F04 outputs. Require the previous
outcome hashes exactly; no new seeds, suffix bank, starts, work limits, or candidate
policy. Expected additional physical work7,766,318 frames, cumulative19,631,012,
below25M. Stop on any discrepancy and diagnose rather than silently rebaseline.
