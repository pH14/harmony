# Hosted NES selector review proposal

The exact two artifact hashes, XGETBV chains and observed incoming-reference results are in nes-selectors.json. Each site uses the Ubuntu glibc2.39 update_active.constprop.0 ECX0 chain, matched to the exact archived compiled libc-start.o. No direct branch enters after zeroing. Indirect/interior entry exclusion is the explicit controlled-program assumption in nes-scope.md, not an inference of arbitrary-code safety. These proposals await primary review.
