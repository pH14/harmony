# SPDX-License-Identifier: AGPL-3.0-or-later
name: Acceptance / Consonance x86

# Hosted-runner x86 acceptance; each replica preserves its own evidence.
on:
  pull_request:
    paths:
      - consonance/**
      - Cargo.toml
      - Cargo.lock
      - rust-toolchain.toml
      - flake.nix
      - flake.lock
      - .github/workflows/x86-virtual-time.yml
  push:
    branches: [main]
    paths:
      - consonance/**
      - Cargo.toml
      - Cargo.lock
      - rust-toolchain.toml
      - flake.nix
      - flake.lock
      - .github/workflows/x86-virtual-time.yml
  schedule:
    - cron: "13 3 * * *"
  workflow_dispatch: {}

concurrency:
  group: x86-virtual-time-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# The explicit `bash` shell runs with pipefail, so `cmd | tee` steps fail when
# the command fails; the implicit default shell would report tee's exit status.
defaults:
  run:
    shell: bash

env:
  CARGO_TERM_COLOR: always
  HARMONY_RUST_TOOLCHAIN: 1.97.0

jobs:
  # X0: CPU identity, /dev/kvm, the KVM capability table, and one minimal
  # real-mode guest through the public KvmBackend.
  probe:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        replica: [1, 2, 3, 4, 5, 6]
    steps:
      - uses: actions/checkout@v4
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal --component rustfmt,clippy
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Build probe
        run: cargo build -p vmm-backend --bin x86_kvm_probe
      # Negative control: with /dev/kvm unreadable the probe must fail closed
      # with the kvm_open reason, never report PASS.
      - name: Probe fails closed without /dev/kvm access
        run: |
          sudo chmod 600 /dev/kvm
          rc=0
          ./target/debug/x86_kvm_probe > probe-negative.txt || rc=$?
          cat probe-negative.txt
          [ "$rc" -eq 1 ]
          grep -q "FAIL_REASON=kvm_open" probe-negative.txt
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          ls -l /dev/kvm
      - name: Run probe
        run: ./target/debug/x86_kvm_probe | tee probe-report.txt
      - name: kvm_smoke (live stock-KVM tests; informational at X0)
        if: always()
        continue-on-error: true
        run: cargo test -p vmm-backend --test kvm_smoke -- --ignored --test-threads=1 2>&1 | tee kvm-smoke.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-probe-replica-${{ matrix.replica }}
          path: |
            probe-report.txt
            probe-negative.txt
            kvm-smoke.txt
          if-no-files-found: error

  # X1: ten same-seed runs of the virtual_time run loop over the stock
  # KvmBackend and a real-mode guest must produce one normalized log, with the
  # comparator and placement-checker negatives standing against the same
  # workload's log. Two replicas so most pushes also sample two hardware draws.
  x1-minimal-guest:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        replica: [1, 2]
    steps:
      - uses: actions/checkout@v4
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal --component rustfmt,clippy
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: X1 live oracles
        run: |
          grep -m1 "model name" /proc/cpuinfo | tee x1-report.txt
          cargo test -p vmm-core --test x86_kvm_virtual_time -- --ignored --test-threads=1 --nocapture 2>&1 | tee -a x1-report.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-x1-replica-${{ matrix.replica }}
          path: x1-report.txt
          if-no-files-found: error

  # X2 input: build the committed guest image from the locked Nix graph in a
  # clean runner store. The output is passed only through this workflow's
  # immutable artifact, never a cross-run cache, so N5 can distinguish an
  # actual rebuild from a restored image.
  guest-image:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
      - name: Install serialization-test runtime
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends qemu-system-x86
      - name: Build guest image from lock
        run: |
          mkdir -p nix-guest-output consonance/harmony-linux/build
          nix run .#guest-images -- \
            --output "$PWD/nix-guest-output" \
            --serialization-gate \
            --n6
          cp nix-guest-output/x86_64/bzImage consonance/harmony-linux/build/bzImage
          cp nix-guest-output/x86_64/initramfs.cpio.gz consonance/harmony-linux/build/initramfs.cpio.gz
          (cd nix-guest-output && sha256sum -c MANIFEST.sha256)
      - name: Record image identity
        run: |
          cp nix-guest-output/MANIFEST.sha256 nix-guest-output/guest-image-report.txt
          cat nix-guest-output/guest-image-report.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-guest-image
          path: nix-guest-output
          if-no-files-found: error

  # N6: the table-generated instruction sweep. The traps-off kernel must be
  # rejected first; only then may both production profiles' two same-seed
  # traps-on boots receive credit.
  n6-instruction-sweep:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    needs: guest-image
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Download locked N6 guest images
        uses: actions/download-artifact@v4
        with:
          name: x86-guest-image
          path: nix-guest-output
      - name: Stage N6 guest images
        run: |
          mkdir -p consonance/harmony-linux/build
          for name in bzImage bzImage-task-park bzImage-n6-traps-off initramfs-n6.cpio.gz initramfs-n6-traps-off.cpio.gz; do
            cp "nix-guest-output/x86_64/$name" consonance/harmony-linux/build/
          done
          (cd nix-guest-output && sha256sum -c guest-image-report.txt)
      - name: Run static N6 positives and negative controls
        run: |
          python3 consonance/harmony-linux/scripts/n6-instruction-sweep.py self-test
          python3 consonance/harmony-linux/scripts/n6-llsc-boundary.py
      - name: Run traps-off negative, then both production JIT sweeps
        run: |
          mkdir -p n6-report
          N6_REPORT_DIR="$PWD/n6-report" timeout -k 10 1800 \
            cargo test --release -p vmm-core --test n6_x86_instruction_sweep \
              -- --ignored --test-threads=1 --nocapture 2>&1 | tee n6-report/test.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-n6-instruction-sweep
          path: n6-report
          if-no-files-found: error

  # #276: ordinary Go must run with no runtime tuning, and its complete
  # execution trace must repeat on both production kernel profiles while the
  # corresponding traps-off boot demonstrably diverges.
  go-runtime:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    needs: guest-image
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        replica: [1, 2]
    steps:
      - uses: actions/checkout@v4
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - uses: actions/download-artifact@v4
        with:
          name: x86-guest-image
          path: nix-guest-output
      - name: Stage Go guest images
        run: |
          mkdir -p consonance/harmony-linux/build
          for name in bzImage bzImage-task-park bzImage-n6-traps-off initramfs-go-runtime.cpio.gz; do
            cp "nix-guest-output/x86_64/$name" consonance/harmony-linux/build/
          done
          (cd nix-guest-output && sha256sum -c guest-image-report.txt)
      - name: Run uninstrumented Go and compare same-seed execution traces
        run: |
          grep -m1 "model name" /proc/cpuinfo | tee go-runtime-report.txt
          timeout -k 10 2100 cargo test --release -p vmm-core --test go_runtime_x86 \
            -- --ignored --test-threads=1 --nocapture 2>&1 | tee -a go-runtime-report.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-go-runtime-${{ matrix.replica }}
          path: go-runtime-report.txt
          if-no-files-found: error

  # X2: Linux boots to /init under assigned-at-exit V-time on the stock
  # backend. Both tiers gate: the smoke tier (one boot) and the determinism
  # tier (10 same-seed boots, one normalized log). The localizer stays
  # informational — it is a diagnostic for when the determinism tier fails.
  # Release build: the trace checkpoints hash the full 256 MiB guest RAM, which
  # an unoptimized sha2 makes wall-clock-prohibitive. Two replicas so most
  # pushes sample two hardware draws.
  x2-linux-virtual_time:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    needs: guest-image
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        # Four replicas: the runner pool draws several CPU models (EPYC 7763,
        # EPYC 9V74, Xeon Platinum 8573C observed) and the vendor mix per push
        # matters while an Intel-only divergence is open.
        replica: [1, 2, 3, 4]
    steps:
      - uses: actions/checkout@v4
      - name: Download locked guest image
        uses: actions/download-artifact@v4
        with:
          name: x86-guest-image
          path: nix-guest-output
      - name: Stage locked guest image
        run: |
          mkdir -p consonance/harmony-linux/build
          cp nix-guest-output/x86_64/bzImage consonance/harmony-linux/build/bzImage
          cp nix-guest-output/x86_64/initramfs.cpio.gz consonance/harmony-linux/build/initramfs.cpio.gz
          (cd nix-guest-output && sha256sum -c guest-image-report.txt)
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal --component rustfmt,clippy
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      # Each tier runs under coreutils `timeout`: the test's own wall watchdog
      # cannot fire while a guest spins exit-free inside a blocking KVM_RUN
      # ioctl, so an external bound keeps a hung boot from consuming the whole
      # job budget — and the tee'd serial up to the hang still uploads.
      - name: X2 smoke (one virtual_time stock boot)
        run: |
          grep -m1 "model name" /proc/cpuinfo | tee x2-report.txt
          X2_LOG_DUMP=$PWD/x2-normalized-log.txt X2_CALIBRATION_LOG=$PWD/x2-boot.calib \
            timeout -k 10 900 cargo test --release -p vmm-core --test x86_kvm_linux_virtual_time \
            x2_virtual_time_stock_boot_smoke -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee -a x2-report.txt
      - name: X2 determinism (10 same-seed boots, one normalized log)
        run: |
          X2_BOOTS=10 timeout -k 10 1500 cargo test --release -p vmm-core --test x86_kvm_linux_virtual_time \
            x2_same_seed_boots_one_normalized_log -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee -a x2-report.txt
      # The localizers are diagnostics: they must run when the determinism
      # tier fails, which is exactly when their output matters.
      - name: X2 first-checkpoint localizer (bounded boots until a divergent pair; informational)
        if: always()
        continue-on-error: true
        run: |
          timeout -k 10 1200 cargo test --release -p vmm-core --test x86_kvm_linux_virtual_time \
            x2_component_diff_first_checkpoint -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee -a x2-report.txt
      - name: X2 divergence localizer (per-component diff; informational)
        if: always()
        continue-on-error: true
        run: |
          timeout -k 10 1200 cargo test --release -p vmm-core --test x86_kvm_linux_virtual_time \
            x2_component_diff_two_boots -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee -a x2-report.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-x2-replica-${{ matrix.replica }}
          path: |
            x2-report.txt
            x2-normalized-log.txt
            x2-boot.calib
          if-no-files-found: error

  # Widen the vendor sample: the pool serves Intel rarely, and every closed
  # cross-vendor divergence needed an Intel draw to observe. Each replica
  # prints its CPU model and runs only the bounded first-checkpoint localizer
  # (sub-second boots), so a wide matrix is cheap. Report-only: an AMD draw
  # finding no divergent pair is the expected common case.
  x2-intel-hunt:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    needs: guest-image
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        replica: [1, 2, 3, 4, 5, 6, 7, 8]
    steps:
      - uses: actions/checkout@v4
      - name: Download locked guest image
        uses: actions/download-artifact@v4
        with:
          name: x86-guest-image
          path: nix-guest-output
      - name: Stage locked guest image
        run: |
          mkdir -p consonance/harmony-linux/build
          cp nix-guest-output/x86_64/bzImage consonance/harmony-linux/build/bzImage
          cp nix-guest-output/x86_64/initramfs.cpio.gz consonance/harmony-linux/build/initramfs.cpio.gz
          (cd nix-guest-output && sha256sum -c guest-image-report.txt)
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal --component rustfmt,clippy
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: First-checkpoint localizer (60 bounded draws)
        run: |
          grep -m1 "model name" /proc/cpuinfo | tee x2-hunt-report.txt
          X2_CKPT_ATTEMPTS=60 timeout -k 10 1200 cargo test --release -p vmm-core --test x86_kvm_linux_virtual_time \
            x2_component_diff_first_checkpoint -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee -a x2-hunt-report.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-x2-hunt-replica-${{ matrix.replica }}
          path: x2-hunt-report.txt
          if-no-files-found: error

  # Linux compile/lint/test truth for the crates the x86 work touches; the
  # macOS development host cannot check cfg(linux) code.
  check:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal --component rustfmt,clippy
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: fmt
        run: cargo fmt --check -p vmm-backend -p vmm-core
      - name: clippy
        run: cargo clippy -p vmm-backend -p vmm-core --all-features --all-targets -- -D warnings
      - name: tests (portable)
        run: cargo test -p vmm-backend --all-features

  pr-smoke:
    name: Consonance real KVM execution, restore, and replay smoke
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Initialize smoke artifact directory
        run: mkdir -p pr-kvm-smoke
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Exercise real hardware, snapshot/restore, and deterministic replay
        run: |
          set -euo pipefail
          mkdir -p pr-kvm-smoke
          timeout -k 10 120 cargo test -p vmm-backend --test kvm_smoke \
            serviced_pio_is_exactly_snapshottable_without_guest_execution -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee pr-kvm-smoke/execution.txt
          grep -Fq 'running 1 test' pr-kvm-smoke/execution.txt
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' pr-kvm-smoke/execution.txt
          timeout -k 10 120 cargo test -p vmm-backend --test kvm_smoke \
            save_restore_round_trips_on_real_kvm -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee pr-kvm-smoke/restore.txt
          grep -Fq 'running 1 test' pr-kvm-smoke/restore.txt
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' pr-kvm-smoke/restore.txt
          timeout -k 10 120 cargo test -p vmm-core --test x86_kvm_virtual_time \
            x1_ten_same_seed_runs_produce_one_normalized_log \
            -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee pr-kvm-smoke/replay.txt
          grep -Fq 'running 1 test' pr-kvm-smoke/replay.txt
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' pr-kvm-smoke/replay.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: consonance-x86-pr-smoke-${{ github.run_id }}
          path: pr-kvm-smoke
          if-no-files-found: error
          retention-days: 14

  # Bounded snapshot/completion qualification on the runner's built product
  # tree. Supported-contract tests are required on every replica; AMD stale-PAE
  # observations remain informational. Every test has an external wall bound.
  snapshot-hardware:
    name: snapshot-hardware replica ${{ matrix.replica }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        replica: [1, 2]
    steps:
      - uses: actions/checkout@v4
      - name: Initialize snapshot evidence
        run: |
          set -euo pipefail
          mkdir -p snapshot-report
          printf '%s\n' 'snapshot-hardware-started' > snapshot-report/job-started.txt
      - name: Install stable toolchain
        run: |
          rustup toolchain install "${HARMONY_RUST_TOOLCHAIN}" --profile minimal
          rustup default "${HARMONY_RUST_TOOLCHAIN}"
      - uses: Swatinem/rust-cache@v2
      - name: Build snapshot gate test binaries once
        env:
          CARGO_TERM_COLOR: never
        run: |
          set -euo pipefail
          cargo test --locked --release -p vmm-backend --lib --test kvm_smoke --no-run \
            2>&1 | tee snapshot-report/build-vmm-backend.log
          cargo test --locked --release -p vmm-core --test x86_cpu_snapshots --no-run \
            2>&1 | tee snapshot-report/build-vmm-core.log
      - name: Grant /dev/kvm access
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          test -c /dev/kvm
          ls -l /dev/kvm
      - name: Record host vendor and paging mode
        id: host
        env:
          SNAPSHOT_REPLICA: ${{ matrix.replica }}
        run: |
          set -euo pipefail
          mkdir -p snapshot-report
          printf '%s\n' "replica=$SNAPSHOT_REPLICA" > snapshot-report/replica.txt
          git rev-parse HEAD > snapshot-report/source-revision.txt
          {
            uname -a
            lscpu
            grep -m1 'model name' /proc/cpuinfo
            grep -m1 'vendor_id' /proc/cpuinfo
          } > snapshot-report/host.txt
          vendor=$(awk -F: '/^vendor_id[[:space:]]*:/{gsub(/[[:space:]]/, "", $2); print $2; exit}' /proc/cpuinfo)
          npt=unavailable
          ept=unavailable
          if test -r /sys/module/kvm_amd/parameters/npt; then
            npt=$(tr -d '[:space:]' < /sys/module/kvm_amd/parameters/npt)
          fi
          if test -r /sys/module/kvm_intel/parameters/ept; then
            ept=$(tr -d '[:space:]' < /sys/module/kvm_intel/parameters/ept)
          fi
          printf 'CPU_VENDOR=%s\nKVM_AMD_NPT=%s\nKVM_INTEL_EPT=%s\n' "$vendor" "$npt" "$ept" | tee snapshot-report/paging-mode.txt
          cat /proc/cpuinfo > snapshot-report/cpuinfo.txt
          amd_npt=false
          intel_ept=false
          if [[ "$vendor" == AuthenticAMD ]]; then
            case "$npt" in
              Y|1) amd_npt=true ;;
              N|0) ;;
              *) echo "Unknown AMD NPT mode: $npt" >&2; exit 1 ;;
            esac
          elif [[ "$vendor" == GenuineIntel ]]; then
            case "$ept" in
              Y|1) intel_ept=true ;;
              N|0) ;;
              *) echo "Unknown Intel EPT mode: $ept" >&2; exit 1 ;;
            esac
          else
            echo "Unknown x86 CPU vendor: $vendor" >&2
            exit 1
          fi
          printf 'vendor=%s\nnpt=%s\nept=%s\namd_npt=%s\nintel_ept=%s\n' "$vendor" "$npt" "$ept" "$amd_npt" "$intel_ept" >> "$GITHUB_OUTPUT"
      - name: Run required KVM completion and full-VMM tests
        env:
          CARGO_TERM_COLOR: never
          AMD_NPT: ${{ steps.host.outputs.amd_npt }}
          CPU_VENDOR: ${{ steps.host.outputs.vendor }}
        run: |
          set -euo pipefail
          mkdir -p snapshot-report
          mkdir -p snapshot-report/npt-guest-write snapshot-report/xsave snapshot-report/xsave-live

          run_exact() {
            local package=$1
            local binary=$2
            local test_name=$3
            local label=$4
            local env_assignment=${5:-}
            local log="snapshot-report/${label}.log"
            local result=0
            local pipeline_status cargo_rc tee_rc
            local command="timeout -k 10 60 cargo test --locked --release -p ${package} --test ${binary} ${test_name} -- --ignored --exact --test-threads=1 --nocapture"
            if [[ -n "$env_assignment" ]]; then
              command="env ${env_assignment} ${command}"
            fi
            printf '%s\n' "$command" | tee "snapshot-report/${label}.command"
            set +e
            if [[ -n "$env_assignment" ]]; then
              env "$env_assignment" timeout -k 10 60 cargo test --locked --release \
                -p "$package" --test "$binary" "$test_name" \
                -- --ignored --exact --test-threads=1 --nocapture 2>&1 | tee "$log"
            else
              timeout -k 10 60 cargo test --locked --release \
                -p "$package" --test "$binary" "$test_name" \
                -- --ignored --exact --test-threads=1 --nocapture 2>&1 | tee "$log"
            fi
            pipeline_status=("${PIPESTATUS[@]}")
            set -e
            cargo_rc=${pipeline_status[0]}
            tee_rc=${pipeline_status[1]}
            if ! printf 'CARGO_EXIT=%s\nTEE_EXIT=%s\n' "$cargo_rc" "$tee_rc" | tee "snapshot-report/${label}.status"; then
              result=1
            fi
            if [[ "$tee_rc" -ne 0 || "$cargo_rc" -ne 0 ]]; then
              result=1
            fi
            if ! grep -Fq 'running 1 test' "$log"; then
              result=1
            fi
            if ! grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' "$log"; then
              result=1
            fi
            return "$result"
          }

          failed=0
          run_exact vmm-backend kvm_smoke serviced_pio_is_exactly_snapshottable_without_guest_execution pio || failed=1
          run_exact vmm-backend kvm_smoke serviced_msr_is_exactly_snapshottable_without_guest_execution msr || failed=1
          run_exact vmm-backend kvm_smoke serviced_exception_payload_round_trips_and_empty_restore_clears_it exception-payload || failed=1
          run_exact vmm-backend kvm_smoke serviced_mmio_is_exactly_snapshottable_across_scalar_rmw_and_movdqu mmio-backend || failed=1
          run_exact vmm-core x86_cpu_snapshots live_kvm::mmio_rmw_finishes_before_full_vmm_snapshot mmio-full-vmm \
            "MMIO_CONTINUATION_REPORT_DIR=$PWD/snapshot-report/mmio-full-vmm" || failed=1
          run_exact vmm-core x86_cpu_snapshots live_kvm::pae_pdpt_reload_preserves_snapshot_continuation pae-reload \
            "PAE_RELOAD_REPORT_DIR=$PWD/snapshot-report/pae-reload" || failed=1
          if [[ "$CPU_VENDOR" == GenuineIntel ]]; then
            run_exact vmm-core x86_cpu_snapshots live_kvm::pae_cached_pdptrs_survive_full_vmm_snapshot_restore pae-full-vmm || failed=1
          fi
          run_exact vmm-backend kvm_smoke resume_flag_preserves_instruction_breakpoint_continuation resume-flag || failed=1
          run_exact vmm-core x86_cpu_snapshots live_kvm::xsave_guest_bytes_survive_cold_continuation xsave \
            "XSAVE_CONTINUATION_REPORT_DIR=$PWD/snapshot-report/xsave" || failed=1
          run_exact vmm-core x86_cpu_snapshots live_kvm::xsave_live_registers_survive_cold_continuation xsave-live \
            "XSAVE_LIVE_REGISTERS_REPORT_DIR=$PWD/snapshot-report/xsave-live" || failed=1
          if [[ "$failed" -ne 0 ]]; then
            printf '%s\n' 'SNAPSHOT_GATE_STATUS=failed' | tee snapshot-report/required-tests.status
            exit 1
          fi
          printf '%s\n' 'SNAPSHOT_GATE_STATUS=passed' | tee snapshot-report/required-tests.status
      - name: Require published snapshot replay and recapture identity
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          printf 'fixed_cpu=%s\n' "$cpu"
          timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-core \
            --test x86_cpu_snapshots live_kvm::public_snapshot_replay_recapture_preserves_xsave_identity \
            -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee snapshot-report/public-snapshot-identity.log
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' snapshot-report/public-snapshot-identity.log
      - name: Record the fixed-CPU raw XRSTOR reuse counterexample
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        env:
          XSAVE_ENTRY_CASE: xcr3-seed0-xrstor
          XSAVE_ENTRY_LONG_MODE: "1"
          XSAVE_ENTRY_REPORT_DIR: ${{ github.workspace }}/snapshot-report/xsave-d1-entry
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          printf 'fixed_cpu=%s\ncase=xcr3-seed0-xrstor\nmode=64-bit\nwarmup=disabled\n' "$cpu" > snapshot-report/xsave-d1.txt
          timeout -k 10 120 taskset -c "$cpu" \
            cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_restores_match_uninterrupted_execution \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-d1.log
      - name: Record the prefaulted raw XRSTOR reuse counterexample
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        env:
          XSAVE_ENTRY_PREFAULT: "1"
          XSAVE_ENTRY_CASE: xcr3-seed0-xrstor
          XSAVE_ENTRY_LONG_MODE: "1"
          XSAVE_ENTRY_REPORT_DIR: ${{ github.workspace }}/snapshot-report/xsave-d1-prefault-entry
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          printf 'fixed_cpu=%s\ncase=xcr3-seed0-xrstor\nmode=64-bit\nwarmup=disabled\nprefault=enabled\ndirty_logging=disabled\n' "$cpu" > snapshot-report/xsave-d1-prefault.txt
          timeout -k 10 120 taskset -c "$cpu" \
            cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_restores_match_uninterrupted_execution \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-d1-prefault.log
      - name: Compare guest-canonicalized XSAVE buffers at complete endpoints
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        env:
          XSAVE_ENTRY_LONG_MODE: "1"
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          printf 'fixed_cpu=%s\nmode=64-bit\nwarmup=disabled\n' "$cpu" > snapshot-report/xsave-d3.txt
          failed=0
          XSAVE_CANONICAL_REPORT_DIR="$PWD/snapshot-report/xsave-d3-original" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::canonical::snapshot_guest_canonicalization_preserves_complete_endpoints \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-d3-original.log || failed=1
          XSAVE_ENTRY_PREFAULT=1 \
          XSAVE_CANONICAL_REPORT_DIR="$PWD/snapshot-report/xsave-d3-prefault" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::canonical::snapshot_guest_canonicalization_preserves_complete_endpoints \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-d3-prefault.log || failed=1
          exit "$failed"
      - name: Require canonicalization to close the raw-entry reuse witness
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        env:
          XSAVE_ENTRY_LONG_MODE: "1"
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          for prefault in 0 1; do
            if [ "$prefault" = 1 ]; then
              export XSAVE_ENTRY_PREFAULT=1
            else
              unset XSAVE_ENTRY_PREFAULT
            fi
            XSAVE_ENTRY_REPORT_DIR="$PWD/snapshot-report/xsave-canonical-reuse-$prefault" \
              timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
                kvm_sys::xsave_diagnostic::snapshot_canonical_entry_restores_match_uninterrupted_execution \
                -- --ignored --exact --test-threads=1 --nocapture \
                2>&1 | tee "snapshot-report/xsave-canonical-reuse-$prefault.log"
            grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' "snapshot-report/xsave-canonical-reuse-$prefault.log"
          done
      - name: Require preparation to preserve state except raw presence
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        run: |
          set -euo pipefail
          timeout -k 10 120 cargo test --locked --release -p vmm-backend --lib \
            kvm_sys::xsave_diagnostic::snapshot_preparation_preserves_state_except_raw_presence \
            -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee snapshot-report/snapshot-preparation.log
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' snapshot-report/snapshot-preparation.log
          timeout -k 10 120 cargo test --locked --release -p vmm-backend --lib \
            kvm_sys::xsave_diagnostic::snapshot_restore_rejects_each_pending_state_without_mutation \
            -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee snapshot-report/snapshot-restore-guards.log
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' snapshot-report/snapshot-restore-guards.log
      - name: Record original init-state MMIO snapshot characterization
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        env:
          MMIO_INIT_REPORT_DIR: ${{ github.workspace }}/snapshot-report/mmio-init-presence
        run: |
          set -euo pipefail
          timeout -k 10 120 cargo test --locked --release -p vmm-core --test x86_cpu_snapshots \
            live_kvm::mmio_init_presence_snapshot_characterization \
            -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee snapshot-report/mmio-init-presence.log
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' snapshot-report/mmio-init-presence.log
      - name: Record raw preparation and post-HLT presence stability
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        run: |
          set -euo pipefail
          set +e
          timeout -k 10 120 cargo test --locked --release -p vmm-backend --lib \
            kvm_sys::xsave_diagnostic::snapshot_preparation_raw_presence_stability \
            -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee snapshot-report/snapshot-preparation-raw.log
          pipeline_status=("${PIPESTATUS[@]}")
          set -e
          printf 'CARGO_EXIT=%s\nTEE_EXIT=%s\n' "${pipeline_status[0]}" "${pipeline_status[1]}" \
            > snapshot-report/snapshot-preparation-raw.status
          test "${pipeline_status[0]}" -eq 0
          test "${pipeline_status[1]}" -eq 0
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' snapshot-report/snapshot-preparation-raw.log
      - name: Compare XSAVE entry across independent and restored executions
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        env:
          XSAVE_ENTRY_REPORT_DIR: ${{ github.workspace }}/snapshot-report/xsave-entry
        run: |
          set -euo pipefail
          timeout -k 10 120 cargo test --locked --release -p vmm-backend --lib \
            kvm_sys::xsave_diagnostic::snapshot_entry_restores_match_uninterrupted_execution \
            -- --ignored --exact --test-threads=1 --nocapture \
            2>&1 | tee snapshot-report/xsave-entry.log
          grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' snapshot-report/xsave-entry.log
      - name: Compare XSAVE restoration and extra entry on one fixed host CPU
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          printf 'fixed_cpu=%s\n' "$cpu" > snapshot-report/xsave-fixed-cpu.txt
          failed=0
          XSAVE_ENTRY_REPORT_DIR="$PWD/snapshot-report/xsave-entry-fixed" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_restores_match_uninterrupted_execution \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-entry-fixed.log || failed=1
          XSAVE_REENTRY_REPORT_DIR="$PWD/snapshot-report/xsave-reentry-fixed" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_debug_reentry_preserves_guest_observation \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-reentry-fixed.log || failed=1
          exit "$failed"
      - name: Compare XSAVE in the shipped guest's 64-bit execution mode
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        env:
          XSAVE_ENTRY_LONG_MODE: "1"
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          printf 'fixed_cpu=%s\nmode=64-bit-long-mode\n' "$cpu" > snapshot-report/xsave-long-mode.txt
          failed=0
          XSAVE_ENTRY_REPORT_DIR="$PWD/snapshot-report/xsave-entry-long-mode" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_restores_match_uninterrupted_execution \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-entry-long-mode.log || failed=1
          XSAVE_REENTRY_REPORT_DIR="$PWD/snapshot-report/xsave-reentry-long-mode" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_debug_reentry_preserves_guest_observation \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-reentry-long-mode.log || failed=1
          exit "$failed"
      - name: Compare all prefaulted XSAVE restore and debug-entry cohorts
        if: ${{ !cancelled() && steps.host.outcome == 'success' }}
        continue-on-error: true
        env:
          XSAVE_ENTRY_PREFAULT: "1"
          XSAVE_ENTRY_LONG_MODE: "1"
        run: |
          set -euo pipefail
          cpu=$(python3 -c 'import os; print(min(os.sched_getaffinity(0)))')
          printf 'fixed_cpu=%s\nmode=64-bit-long-mode\nprefault=enabled\ndirty_logging=disabled\n' "$cpu" > snapshot-report/xsave-prefault.txt
          failed=0
          XSAVE_ENTRY_REPORT_DIR="$PWD/snapshot-report/xsave-entry-prefault" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_restores_match_uninterrupted_execution \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-entry-prefault.log || failed=1
          XSAVE_REENTRY_REPORT_DIR="$PWD/snapshot-report/xsave-reentry-prefault" \
            timeout -k 10 120 taskset -c "$cpu" cargo test --locked --release -p vmm-backend --lib \
              kvm_sys::xsave_diagnostic::snapshot_entry_debug_reentry_preserves_guest_observation \
              -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee snapshot-report/xsave-reentry-prefault.log || failed=1
          exit "$failed"
      - name: Record AMD stale PAE behavior outside the supported guest contract
        if: ${{ !cancelled() && steps.host.outcome == 'success' && steps.host.outputs.amd_npt == 'true' }}
        continue-on-error: true
        env:
          NPT_REPORT_DIR: ${{ github.workspace }}/snapshot-report/npt-guest-write
        run: |
          set -euo pipefail
          failed=0
          for name in pae_cached_pdptrs_survive_full_vmm_snapshot_restore amd_default_npt_pae_guest_write_observations; do
            set +e
            timeout -k 10 60 cargo test --locked --release -p vmm-core --test x86_cpu_snapshots \
              "live_kvm::$name" -- --ignored --exact --test-threads=1 --nocapture \
              2>&1 | tee "snapshot-report/$name.log"
            pipeline_status=("${PIPESTATUS[@]}")
            set -e
            printf 'CARGO_EXIT=%s\nTEE_EXIT=%s\n' "${pipeline_status[0]}" "${pipeline_status[1]}" \
              > "snapshot-report/$name.status"
            if [[ "${pipeline_status[0]}" -ne 0 || "${pipeline_status[1]}" -ne 0 ]]; then
              failed=1
            fi
            grep -Fq 'test result: ok. 1 passed; 0 failed; 0 ignored;' "snapshot-report/$name.log" || failed=1
          done
          exit "$failed"
      - name: Explain informational XSAVE results
        if: always()
        run: |
          printf '%s\n' \
            'Raw XSAVE entry, reentry and preparation-presence checks are informational hardware diagnostics on every host.' \
            'Informational failures are retained failures, not passes. Raw bitmap identity remains unresolved; production snapshot serialization is unchanged.' \
            'Preparation invariants excluding raw presence, D3 canonical endpoints and full-VMM snapshot checks remain required.' \
            >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x86-snapshot-hardware-${{ github.run_id }}-replica-${{ matrix.replica }}
          path: snapshot-report
          if-no-files-found: error
          retention-days: 30
