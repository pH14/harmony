# SPDX-License-Identifier: AGPL-3.0-or-later
#
# Frozen instruction-surface table for docs/DETERMINISM.md.
# A row is the unit of the N6 generated sweep. `claim` is deliberately limited
# to the two claims N6 permits: execute the handled operation, or prove that an
# unreachable entropy operation is both masked and rejected by the image audit.

schema = 1

[[instruction]]
id = "arm64-counter-frequency"
arch = "arm64"
channel = "time-and-identity"
operations = ["MRS CNTFRQ_EL0"]
claim = "execute"
layers = ["protocol", "image-audit"]
disposition = "The owned kernel obtains the fixed frequency from the Harmony contract; executable-image scans reject a raw read."
evidence = ["docs/VM-EXIT-COUNT-VTIME-STATUS.md#M1--the-M1-Max-boots-deterministically"]
verification = "tested-image-audit; generated hostile probe untested"

[[instruction]]
id = "arm64-virtual-counter"
arch = "arm64"
channel = "time"
operations = ["MRS CNTVCT_EL0", "MRS CNTVCTSS_EL0"]
claim = "execute"
layers = ["protocol", "image-audit", "guest-kernel-trap"]
disposition = "Owned code reads the pvclock page, executable-image scans reject raw reads, and EL0 counter access is disabled."
evidence = ["consonance/harmony-linux/linux/patches/arm64/0002-arm64-harmony-pvclock-exit-count-clocksource.patch", "docs/VM-EXIT-COUNT-VTIME-STATUS.md#M1--the-M1-Max-boots-deterministically"]
verification = "tested-image-audit; traps-off negative and JIT probe untested"

[[instruction]]
id = "arm64-physical-counter"
arch = "arm64"
channel = "time"
operations = ["MRS CNTPCT_EL0", "MRS CNTPCTSS_EL0"]
claim = "execute"
layers = ["protocol", "image-audit", "guest-kernel-trap"]
disposition = "Owned code reads the pvclock page, executable-image scans reject raw reads, and EL0 physical-counter access is disabled."
evidence = ["consonance/harmony-linux/linux/patches/arm64/0002-arm64-harmony-pvclock-exit-count-clocksource.patch", "docs/VM-EXIT-COUNT-VTIME-STATUS.md#M1--the-M1-Max-boots-deterministically"]
verification = "tested-image-audit; traps-off negative and JIT probe untested"

[[instruction]]
id = "arm64-live-timer-programming"
arch = "arm64"
channel = "time"
operations = ["MSR CNTV_TVAL_EL0", "MSR CNTV_CVAL_EL0", "MSR CNTP_TVAL_EL0", "MSR CNTP_CVAL_EL0"]
claim = "execute"
layers = ["protocol", "image-audit"]
disposition = "The owned kernel programs the deterministic Harmony clockevent; executable-image scans reject programming the host counter domains."
evidence = ["consonance/harmony-linux/scripts/aa5-counter-scan.py", "docs/VM-EXIT-COUNT-VTIME-STATUS.md#M1--the-M1-Max-boots-deterministically"]
verification = "tested-image-audit; generated hostile probe untested"

[[instruction]]
id = "arm64-cache-identity"
arch = "arm64"
channel = "implementation-identity"
operations = ["MRS CTR_EL0", "MRS DCZID_EL0"]
claim = "execute"
layers = ["protocol", "hypervisor-policy"]
disposition = "The backend installs the frozen arm64 identity values."
evidence = ["consonance/vmm-core/src/vendor/arm64/contract.rs", "docs/VM-EXIT-COUNT-VTIME-STATUS.md#M5--bidirectional-HVFKVM-determinism-and-snapshot-portability"]
verification = "cross-host workload evidence exists; exhaustive generated read probe untested"

[[instruction]]
id = "arm64-id-registers"
arch = "arm64"
channel = "implementation-identity"
operation-expansions = ["MRS ID_AA64AFR0_EL1", "MRS ID_AA64AFR1_EL1"]
operations = ["MRS ID_AA64PFR0_EL1", "MRS ID_AA64PFR1_EL1", "MRS ID_AA64PFR2_EL1", "MRS ID_AA64ZFR0_EL1", "MRS ID_AA64SMFR0_EL1", "MRS ID_AA64FPFR0_EL1", "MRS ID_AA64DFR0_EL1", "MRS ID_AA64DFR1_EL1", "MRS ID_AA64DFR2_EL1", "MRS ID_AA64ISAR0_EL1", "MRS ID_AA64ISAR1_EL1", "MRS ID_AA64ISAR2_EL1", "MRS ID_AA64ISAR3_EL1", "MRS ID_AA64MMFR0_EL1", "MRS ID_AA64MMFR1_EL1", "MRS ID_AA64MMFR2_EL1", "MRS ID_AA64MMFR3_EL1", "MRS ID_AA64MMFR4_EL1", "MRS MIDR_EL1", "MRS MPIDR_EL1", "MRS REVIDR_EL1", "MRS AIDR_EL1"]
claim = "execute"
layers = ["protocol", "hypervisor-policy", "image-audit"]
disposition = "The backend installs the frozen identity model; implementation revision reads that are not safely virtualized are rejected from owned executable images."
evidence = ["consonance/vmm-core/src/vendor/arm64/contract.rs", "consonance/harmony-linux/scripts/aa5-counter-scan.py", "docs/VM-EXIT-COUNT-VTIME-STATUS.md#M5--bidirectional-HVFKVM-determinism-and-snapshot-portability"]
verification = "cross-host workload evidence and selected image audit exist; complete generated listing and probe untested"

[[instruction]]
id = "arm64-pmu"
arch = "arm64"
channel = "machine-measurement"
operation-expansions = ["MSR PMEVCNTR[0-30]_EL0", "MSR PMEVTYPER[0-30]_EL0"]
operations = ["MRS PMCR_EL0", "MSR PMCR_EL0", "MRS PMCCNTR_EL0", "MSR PMCCNTR_EL0", "MRS PMCNTENSET_EL0", "MSR PMCNTENSET_EL0", "MRS PMCNTENCLR_EL0", "MSR PMCNTENCLR_EL0", "MRS PMOVSCLR_EL0", "MSR PMOVSCLR_EL0", "MRS PMOVSSET_EL0", "MSR PMOVSSET_EL0", "MRS PMSELR_EL0", "MSR PMSELR_EL0", "MRS PMXEVTYPER_EL0", "MSR PMXEVTYPER_EL0", "MRS PMXEVCNTR_EL0", "MSR PMXEVCNTR_EL0", "MRS PMUSERENR_EL0", "MSR PMUSERENR_EL0", "MRS PMCCFILTR_EL0", "MSR PMCCFILTR_EL0", "MSR PMSWINC_EL0", "MRS PMEVCNTR0_EL0", "MRS PMEVCNTR1_EL0", "MRS PMEVCNTR2_EL0", "MRS PMEVCNTR3_EL0", "MRS PMEVCNTR4_EL0", "MRS PMEVCNTR5_EL0", "MRS PMEVCNTR6_EL0", "MRS PMEVCNTR7_EL0", "MRS PMEVCNTR8_EL0", "MRS PMEVCNTR9_EL0", "MRS PMEVCNTR10_EL0", "MRS PMEVCNTR11_EL0", "MRS PMEVCNTR12_EL0", "MRS PMEVCNTR13_EL0", "MRS PMEVCNTR14_EL0", "MRS PMEVCNTR15_EL0", "MRS PMEVCNTR16_EL0", "MRS PMEVCNTR17_EL0", "MRS PMEVCNTR18_EL0", "MRS PMEVCNTR19_EL0", "MRS PMEVCNTR20_EL0", "MRS PMEVCNTR21_EL0", "MRS PMEVCNTR22_EL0", "MRS PMEVCNTR23_EL0", "MRS PMEVCNTR24_EL0", "MRS PMEVCNTR25_EL0", "MRS PMEVCNTR26_EL0", "MRS PMEVCNTR27_EL0", "MRS PMEVCNTR28_EL0", "MRS PMEVCNTR29_EL0", "MRS PMEVCNTR30_EL0", "MRS PMEVTYPER0_EL0", "MRS PMEVTYPER1_EL0", "MRS PMEVTYPER2_EL0", "MRS PMEVTYPER3_EL0", "MRS PMEVTYPER4_EL0", "MRS PMEVTYPER5_EL0", "MRS PMEVTYPER6_EL0", "MRS PMEVTYPER7_EL0", "MRS PMEVTYPER8_EL0", "MRS PMEVTYPER9_EL0", "MRS PMEVTYPER10_EL0", "MRS PMEVTYPER11_EL0", "MRS PMEVTYPER12_EL0", "MRS PMEVTYPER13_EL0", "MRS PMEVTYPER14_EL0", "MRS PMEVTYPER15_EL0", "MRS PMEVTYPER16_EL0", "MRS PMEVTYPER17_EL0", "MRS PMEVTYPER18_EL0", "MRS PMEVTYPER19_EL0", "MRS PMEVTYPER20_EL0", "MRS PMEVTYPER21_EL0", "MRS PMEVTYPER22_EL0", "MRS PMEVTYPER23_EL0", "MRS PMEVTYPER24_EL0", "MRS PMEVTYPER25_EL0", "MRS PMEVTYPER26_EL0", "MRS PMEVTYPER27_EL0", "MRS PMEVTYPER28_EL0", "MRS PMEVTYPER29_EL0", "MRS PMEVTYPER30_EL0"]
claim = "execute"
layers = ["protocol", "guest-kernel-trap", "hypervisor-policy"]
disposition = "The frozen identity exposes no guest PMU; EL0 access is disabled and substrate policy must not expose host counters."
evidence = ["docs/VM-EXIT-COUNT-VTIME-STATUS.md#M1--the-M1-Max-boots-deterministically"]
verification = "HVF PMCCNTR trap measured; complete generated register sweep and traps-off negative untested"

[[instruction]]
id = "arm64-entropy"
arch = "arm64"
channel = "entropy"
operations = ["MRS RNDR", "MRS RNDRRS"]
claim = "mask-and-audit"
layers = ["protocol", "image-audit"]
disposition = "The frozen identity hides FEAT_RNG and admitted images must reject the opcodes; a binary that ignores the feature bits is outside the cooperative contract."
evidence = ["consonance/vmm-core/src/vendor/arm64/contract.rs"]
verification = "untested: opcode audit and hostile feature-mask probe are N6 work"

[[instruction]]
id = "arm64-exclusive-monitor"
arch = "arm64"
channel = "host-scheduling-residue"
operations = ["LDXR", "LDAXR", "LDXP", "LDAXP", "STXR", "STLXR", "STXP", "STLXP"]
claim = "execute"
layers = ["protocol", "image-audit"]
disposition = "Only side-effect-free EL0 retry loops are admitted by argument; the owned kernel and retry-observing loops remain prohibited, and the latter is a documented cooperative residual."
evidence = ["consonance/harmony-linux/scripts/aa4-exclusive-scan.py", "docs/VM-EXIT-COUNT-VTIME-STATUS.md#M1--the-M1-Max-boots-deterministically"]
verification = "tested-image-audit; noisy/quiet convergence and accumulating-retry negative untested"

[[instruction]]
id = "x86-cpuid"
arch = "x86_64"
channel = "implementation-identity"
operations = ["CPUID"]
claim = "execute"
layers = ["protocol", "hypervisor-policy"]
disposition = "Every leaf is answered from the frozen CPUID model with hardware RNG hidden in the stock VM-exit-count composition."
evidence = ["https://github.com/pH14/harmony/blob/57b16ce0/docs/PRESCRIPTIVE-VTIME-STATUS.md#recorded-decisions-x86"]
verification = "tested by X2/X3 cross-vendor boots; exhaustive generated leaf probe untested"

[[instruction]]
id = "x86-tsc"
arch = "x86_64"
channel = "time"
operations = ["RDTSC", "RDTSCP"]
claim = "execute"
layers = ["protocol", "image-audit", "guest-kernel-trap"]
disposition = "Owned code uses the pvclock page and the executable-image scan accounts for raw reads; unaudited ring-3 code requires CR4.TSD handling."
evidence = ["https://github.com/pH14/harmony/blob/57b16ce0/docs/PRESCRIPTIVE-VTIME-STATUS.md#recorded-decisions-x86"]
verification = "tested for the owned X2 image; CR4.TSD completion, JIT probe, and traps-off negative untested"

[[instruction]]
id = "x86-pmu"
arch = "x86_64"
channel = "machine-measurement"
operations = ["RDPMC"]
claim = "execute"
layers = ["protocol", "guest-kernel-trap", "hypervisor-policy"]
disposition = "CPUID exposes no vPMU, CR4.PCE remains clear for ring 3, and stock KVM intercepts RDPMC."
evidence = ["docs/CPU-MSR-CONTRACT.md#4-instruction-and-vmx-control-disposition", "https://github.com/pH14/harmony/blob/57b16ce0/docs/PRESCRIPTIVE-VTIME-STATUS.md#recorded-decisions-x86"]
verification = "contract and X-series boot evidence exist; JIT probe and traps-off negative untested"

[[instruction]]
id = "x86-entropy"
arch = "x86_64"
channel = "entropy"
operations = ["RDRAND", "RDSEED"]
claim = "mask-and-audit"
layers = ["protocol", "image-audit"]
disposition = "The stock composition hides both CPUID bits and admitted images allow the opcodes only behind those feature checks; an unaudited binary that ignores CPUID is outside the cooperative contract."
evidence = ["https://github.com/pH14/harmony/blob/57b16ce0/docs/PRESCRIPTIVE-VTIME-STATUS.md#recorded-decisions-x86"]
verification = "tested for the X2 kernel image; initramfs audit and hostile mask probe untested"

[[instruction]]
id = "x86-monitor-mwait"
arch = "x86_64"
channel = "host-power-and-wake"
operations = ["MONITOR", "MWAIT"]
claim = "execute"
layers = ["protocol", "hypervisor-policy"]
disposition = "CPUID hides MONITOR and the substrate intercept backstop produces a deterministic fault."
evidence = ["docs/CPU-MSR-CONTRACT.md#4-instruction-and-vmx-control-disposition"]
verification = "contract tests exist; generated in-guest probe untested"

[[instruction]]
id = "x86-waitpkg"
arch = "x86_64"
channel = "host-time-and-wake"
operations = ["UMONITOR", "UMWAIT", "TPAUSE"]
claim = "execute"
layers = ["protocol", "hypervisor-policy"]
disposition = "CPUID hides WAITPKG and the substrate keeps user-wait execution disabled, producing a deterministic fault."
evidence = ["docs/CPU-MSR-CONTRACT.md#4-instruction-and-vmx-control-disposition"]
verification = "untested on the consolidated tree"

[[instruction]]
id = "x86-xsave-image"
arch = "x86_64"
channel = "implementation-identity"
operations = ["FXSAVE", "FXSAVE64", "XSAVE", "XSAVEOPT", "XSAVEC", "XSAVES"]
claim = "execute"
layers = ["guest-kernel-pin", "hypervisor-save-canonicalization"]
disposition = "MXCSR_MASK, init-state component encodings, and ignored legacy-tail bytes are canonicalized at their guest and VMM save boundaries."
evidence = ["https://github.com/pH14/harmony/blob/57b16ce0/docs/PRESCRIPTIVE-VTIME-STATUS.md#recorded-decisions-x86"]
verification = "tested by X3 cross-vendor boots; exhaustive instruction-form probe untested"

[[instruction]]
id = "x86-exit-rflags-rf"
arch = "x86_64"
channel = "exit-mechanics"
operations = ["exit-time RFLAGS.RF"]
claim = "execute"
layers = ["hypervisor-save-canonicalization"]
disposition = "The save boundary clears RF because VMX and SVM expose different unreadable mid-instruction residue."
evidence = ["https://github.com/pH14/harmony/blob/57b16ce0/docs/PRESCRIPTIVE-VTIME-STATUS.md#recorded-decisions-x86"]
verification = "tested by X3 cross-vendor boots; dedicated generated exit probe untested"

[[instruction]]
id = "x86-undefined-af"
arch = "x86_64"
channel = "undefined-flags"
operations = ["shift followed by interrupt-frame capture", "multiply followed by interrupt-frame capture", "PUSHF capture", "PUSHFQ capture", "SYSCALL R11 capture"]
claim = "execute"
layers = ["guest-kernel-pin"]
disposition = "The Harmony guest clears AF at the three 64-bit kernel capture funnels before architecturally undefined vendor residue reaches retained memory."
evidence = ["https://github.com/pH14/harmony/blob/57b16ce0/docs/PRESCRIPTIVE-VTIME-STATUS.md#recorded-decisions-x86"]
verification = "tested by X3 cross-vendor boots; dedicated generated capture probes untested"
