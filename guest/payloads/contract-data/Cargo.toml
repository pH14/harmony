[package]
name = "contract-data"
version = "0.1.0"
edition.workspace = true
license.workspace = true
build = "build.rs"

# Conformance tables (CPUID model, allowed/denied MSRs, frozen frequencies)
# generated at build time from docs/cpu-msr-contract.toml by build.rs — so a
# contract bump surfaces as a payload diff, never a silent drift. Shared verbatim
# between the bare-metal sweep payloads (no_std) and host-side tests, exactly like
# compute-core. No dependencies: build.rs parses the contract's strict grammar
# with std only (no TOML crate needed, keeping within the conventions whitelist).
[dependencies]
