/target
/harmony-linux/build
/harmony-linux/dl
/harmony-linux/libvoidstar/build
/consonance/acceptance-suite/payloads/target
*.img
*.bin
!consonance/acceptance-suite/golden/*.bin
/.claude/skills/pr-review-workspace/
.agent-prompt.md
.env
# Spikes are worktrees on spike/* branches, never directories on main
# (AGENTS.md, Conventions). Historical spike trees live on the spike/*
# branches; local spike checkouts and build litter stay untracked here.
spikes/
/mutants.out
/mutants.out.old

# Run/telemetry outputs (e.g. k3s-run.*.ndjson) are data, not source.
*.ndjson
*.log

.DS_Store

# Parked (2026-08-10): macOS-backend exploration stays untracked until ratified.
# The QEMU TCG spike itself is ignored by the spikes/ rule above.
docs/QEMU-TCG-EXACT-BRANCH-CLOCK-SPIKE.md
docs/MACOS-M3-BACKEND-ALTERNATIVES.md
flake.nix
