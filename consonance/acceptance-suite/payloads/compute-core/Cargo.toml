# The deterministic integer workload behind the `compute` payload, compiled
# in two worlds: as a no_std dependency of the payload (x86_64-unknown-none)
# and host-side, where its test recomputes the digest and checks it against
# consonance/acceptance-suite/golden/compute.txt. run-tests.sh runs that test with --target <host>.
[package]
name = "compute-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
