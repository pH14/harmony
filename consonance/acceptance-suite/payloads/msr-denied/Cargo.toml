[package]
name = "msr-denied"
version = "0.1.0"
edition.workspace = true
license.workspace = true
build = "build.rs"

[dependencies]
common = { path = "../common" }
contract-data = { path = "../contract-data" }
