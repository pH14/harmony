[package]
name = "environment"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Deterministic supplies, opaque service channels, and input reproducers"

[dependencies]
thiserror = "2"

[lints]
workspace = true
