# Task 120 — Differential Dataflow lineage/evidence-cut/retention spike (`hm-bbx.2`)

The de-risking spike for the Differential observation/materialization migration (epic
`hm-bbx`, ruled by `docs/DISSONANCE-STRATEGY.md`; lane unblocked by Paul 2026-07-16).
**This is a spike**: bounded, fixture-driven, standalone under `spikes/` — it proves the
hardest queries before any production integration. Claim `hm-bbx.2` first
(`bd update hm-bbx.2 --claim`).

## Deliverable (from the bead, binding)

A bounded Differential Dataflow program over **persisted fixture records** proving:

- **Lineage-complete observation prefixes at candidate seals**;
- **provisional transitions at configured unsealed evidence cuts** that can nominate
  replay but **never occupancy**;
- **sibling-safe rollout identity**; **same-Moment cuts**; **canonical order
  reconstruction**;
- **set/max/min/accumulate and history derivations**; **property-level assertion
  aggregation**;
- separation of **immutable evidence / bounded working membership / committed Entry
  assignments / finalized facts**.

Measure **arrangement sharing** (the cost story is a first-class deliverable, not an
afterthought — report per-branch incremental cost vs recompute).

## Doctrine (ruled — violations are blocking)

- **Branch is a KEY in the dataflow lifecycle, NEVER a timestamp.**
- **Revision is time; Moment/ordinal is data.**
- **No custom lattice.**

## Context you must read first

- `docs/DISSONANCE-STRATEGY.md` (the ruling; the three-species model and the spike's
  place in the epic).
- The epic bead (`bd show hm-bbx`) and its child structure — your spike's findings gate
  the explicit ratification decision (`hm-bbx.5`) downstream; write them so a GO/NO-GO
  can be judged from the PR alone.
- The prior register-linearizability fork-oracle spike's conclusions (2026-07-07):
  Window fork-seeding gave flat per-branch cost (9.4× less work at depth 3), 1:1
  Stateright parity on all branches; the RunTrace suffix-only cross-fork contract gap is
  a known landmine — state how your design addresses or sidesteps it.

## Rules

Spike conventions: standalone Cargo project under `spikes/differential-lineage/` with
its own manifest (not in the workspace); fixtures committed (small, deterministic,
generated by a committed generator); `differential-dataflow`/`timely` are the expected
dependencies (declare versions in the PR description). Determinism discipline still
applies to fixture generation and any hashing. No box work — Mac-local throughout.

## Gates

`cargo build/test/clippy -D warnings/fmt` on the spike manifest; every proven query has
a fixture-backed test with exact expected outputs; the arrangement-sharing measurement
is reproducible by one documented command. Findings + GO/NO-GO recommendation live in
the PR description (no docs/history file).

## Definition of done

All eight query families proven over fixtures with exact assertions; cost measured and
reported; doctrine compliance explicit; `hm-bbx.2` closes on merge, feeding the
ratification decision `hm-bbx.5`.
