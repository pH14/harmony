[package]
name = "runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# The minimal arm64 bare-metal runtime the oracle payloads boot on: boot shim,
# MMU, exception vectors, PL011 console, GICv3 init, the params/pvclock shared
# pages, and the exit protocol. Spike apparatus — `unsafe` is expected and
# granted here (task 109 §Constraints).
[dependencies]
oracle-model = { path = "../../oracle-model" }
