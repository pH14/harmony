AA-6 mini determinism gate — live, 2026-07-20, Ampere Altra N1
host: 6.18.35-aa4guard (patched KVM_EXIT_PREEMPT), build-id ac576f87, core 60
mechanism: patched (Preempt), skid_margin 53, --with-targets, 6 payloads x reps 1000 = 6000 records
floor-check --min-reps 1000:
  rep-floor PASS (6 inputs x >=1000 reps), replay-identity PASS (6 groups bit-identical
  state digests), multiplicity PASS (6000 armed overflows delivered exactly once),
  skid PASS (no overshoot at margin 53 — 53 holds for BARE payloads under patched mech),
  mechanism-attestation PASS (Preempt).
  FAIL: weights-present/count-exactness (no AA-1 weights pack; determinism is the gate here),
        aa6-matrix (needs determinism UNDER fault injection across all classes incl.
        linux-guest — the larger AA-6 deliverable, not the mini-gate).
VERDICT: AA-6 mini determinism gate (>=1000-rep bit-identity) DEMONSTRATED on silicon.
records.jsonl content-addressed on box (run-set.json records_sha256).

--- machine-checkable verdict (added 2026-07-25, bead hm-7q0) ---
The prose VERDICT above is now checkable by a SCOPED floor-check invocation that rests the
exit code on ONLY the five mini-gate checks and exits 0 iff every one PASSES, while the two
transparently-larger deliverables (weights/count-exactness, aa6-matrix) are reported but do
not gate:

  floor-check <run-set-dir> --min-reps 1000 \
    --scope rep-floor --scope replay-identity --scope multiplicity \
    --scope skid --scope mechanism-attestation

Exit 0 == "the AA-6 mini determinism gate is DEMONSTRATED for this run-set" — no prose
reconciliation of a full-run FAIL. The scoped run prints RESULT: [SCOPED] PASS and lists the
out-of-scope FAILs on the face of the verdict, so it can never be read as a full stage
acceptance. Demonstrated exit-0 over the committed sibling run-set
results/aa-6/live-20260721/aa6-minigate (8000 records, real evidence): full run FAILs only
aa6-matrix; the scoped invocation above exits 0. The 2026-07-20 run-set's records are
content-addressed on box, so re-running the command here needs the box; the capability and its
negative control (a FAILing check pulled into scope drives it RED) are gated in CI by
floor-check's accept_reject suite.
