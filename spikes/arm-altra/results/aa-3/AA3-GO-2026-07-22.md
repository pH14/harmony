# AA-3 — GO (regenerated-pin basis, Paul-ruled 2026-07-22)

The AA-3 GO (voided 2026-07-18 for an integrity omission — comparators not invoked) is
**re-issued GO**. Acceptance was met on-silicon with the comparators properly invoked, and
Paul ruled the regenerated-pin basis acceptable ("for ARM, we need to just go. Let's accept
the regenerated pins, and get going to AA-6"). This is a Paul-ruled GO on the accepted basis,
not a new determinism claim.

**Acceptance evidence (task 137, hm-idb):**
- `recert-20260721/full/` — the ≥10⁶ acceptance run: `floor-check PASS (1371 checks)`,
  1,010,800 armed overflows, 505,400 distinct cases, skid=0 exact, Preempt attested;
  solo==co-tenant `MATCH` 5700/5700, 0 divergences.
- `sweep-01/`…`sweep-11/` — 11 further independent ≥10⁶ cycles on fresh seed bases
  (`…0100`–`…1100`), every one `PASS (1371 checks)` + `solo==co-tenant MATCH`.
- **Total: 12 green ≥10⁶ campaigns, ~12.1M armed overflows, solo==co-tenant held every
  cycle, zero P0.**

**Basis Paul accepted:** the box wipe (2026-07-20) + toolchain reinstall (different physical
Altra) made the certified payload *bytes* (`../aa-1b/inputs/payload-pins.json`)
non-reproducible; the campaign used **regenerated pins** from a fresh build of the
git-verified byte-identical certified source, with `count-exactness` (`work==target`) as the
byte-pin-independent semantic gate. Durable prevention tracked: **hm-nji6** (reproducible +
archived pins), **hm-gfr1** (static/reproducible hardware + toolchain definition).

Full disposition: `docs/ARM-ALTRA.md` §AA-3 and `recert-20260721/STATUS.md`.
The per-cycle `sweep-NN/README.txt` files carry the pre-ruling *diagnostic* framing (accurate
as written, before the ruling); this note is the authoritative GO record for the sweep legs.
