[package]
name = "instrumentor-agent"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
fuzzer = { version = "0.1.0", path = "../fuzzer" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.0"
