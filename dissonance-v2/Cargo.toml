# Standalone workspace root, deliberately outside the harmony root workspace
# (same pattern as spikes/differential-lineage). See CLAUDE.md in this
# directory before working here.
[workspace]
resolver = "2"
members = ["fuzzer", "instrumentor-agent", "triage-agent"]

[workspace.package]
edition = "2024"
license = "AGPL-3.0-or-later"
