[package]
name = "environment"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "The decide seam, fault catalog, SeededEnv, and recorded-replay format (dissonance task 24)"

[lints]
workspace = true

[dependencies]
thiserror = "2"

[dev-dependencies]
proptest = "1"
